function getResultFromOpID(): string {
  let content = `// Retrieve the events and outPuts from the operation ID\n`;
  // TODO: Retrieve the events and outPuts from the operation ID

  return content;
}
